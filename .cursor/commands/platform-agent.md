# Platform Agent

Act as the **Platform / Shell Agent** for `apps/admin`. Apply the full scope, rules, and working style in **`.cursor/agents/platform-shell.md`**.

Also respect project rules under `.cursor/rules/` and [`AGENTS.md`](../../AGENTS.md) (FSD, i18n, page loading UX, testing, etc.). Do not own domain-specific business logic—defer to the domain agents listed in `platform-shell.md`.

**Your task:** _(describe routing, layout, sidebar, auth gates, login/not-found, sub-page configs, or app-wide shell changes)_
