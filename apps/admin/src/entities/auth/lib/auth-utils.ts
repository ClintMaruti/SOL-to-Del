/**
 * Authentication utilities
 *
 * Note: With httpOnly cookies, tokens are managed server-side only.
 * The client cannot read or write authentication tokens directly.
 * All token operations are handled via HTTP-only cookies set by the server.
 */
