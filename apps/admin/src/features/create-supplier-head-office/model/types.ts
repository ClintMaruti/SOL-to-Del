/**
 * Form data shape for Create Supplier Head Office (empty initial state).
 */
export interface CreateSupplierHeadOfficeFormData {
  name: string;
  email: string;
  phoneNumber: string;
  additionalEmail: string;
  website: string;
  country: string;
  city: string;
  postalCode: string;
  streetAddress: string;
}
